

// accept an argument, return an array of objects

var ResourceArrayService = angular.module('ResourceArrayService', [])
.service('ResourceArray', function() {
  //parse the requirements
  /*
    return favorites - make a special function
    filter by type
    filter by subject
    filter by ages
    filter by name


    request the resources from mongo
    enter the resources into an array
    return the array
  */
})
